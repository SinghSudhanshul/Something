{
  "name": "@nexus/web",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "echo 'Next.js web app — scaffold only this week' && exit 0",
    "build": "echo 'Next.js build — scaffold only' && exit 0",
    "type-check": "echo 'No TypeScript files yet' && exit 0",
    "lint": "echo 'No source files yet' && exit 0",
    "test": "echo 'No tests yet' && exit 0"
  },
  "dependencies": {
    "@nexus/types": "workspace:*"
  }
}
